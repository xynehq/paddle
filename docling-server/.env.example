# Docling OCR Service Configuration

# Server port (inside container)
PORT=8000

# Logging level
LOG_LEVEL=INFO

# Optional: Model artifacts path for offline mode
# DOCLING_ARTIFACTS_PATH=/path/to/models
